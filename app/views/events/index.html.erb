<p id="notice"><%= notice %></p>

<h1>Events</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Date</th>
      <th>Poster</th>
      <th colspan="3"></th>
    </tr>
    <tr>
<form class="mainnav-form" role="search" action="/events" method="get">
  <input type="text" class="form-control input-md mainnav-search-query" name="q" placeholder="Search Events" value="<%= @previous_search %>">
  <button class="btn btn-sm mainnav-form-btn"><i class="fa fa-search"></i></button>
</form>
    </tr>
  </thead>

  <tbody>
    <% if @events %>
      <% @events.each do |event| %>
        <tr>
          <td><%= link_to event.name, event_path(event)%></td>
          <td><%= event.description %></td>
          <td><%= event.date %></td>
          <td><%= link_to event.name, controller: "events", action: "show", id: event.id%></td>
        </tr>
      <% end %>
    <% end %>

    <% if @search_results %>
      <% @search_results.each do |event| %>
        <tr>
          <td><%= link_to event.name, controller: "events", action: "show", id: event._id%></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<br>
<%= link_to 'New Event post', new_event_path %>
