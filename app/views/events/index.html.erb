<p id="notice"><%= notice %></p>

<h1>Events</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Date</th>
      <th>Poster</th>
      <th colspan="3"></th>
    </tr>
    <tr>
<form class="mainnav-form" role="search" action="/events" method="get">
  <input type="text" class="form-control input-md mainnav-search-query" name="search" placeholder="Search Events" value="<%= @previous_search %>">
  <button class="btn btn-sm mainnav-form-btn"><i class="fa fa-search"></i></button>
</form>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= link_to event.name, event_path(event)%></td>
        <td><%= event.description %></td>
        <td><%= event.date %></td>
        <td><%= link_to event.poster_name, user_path(event.user) %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<br>
<%= link_to 'New Event post', new_event_path %>
